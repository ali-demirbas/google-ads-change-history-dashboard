---
name: Feature request
about: A source format, category rule, or dashboard component is missing
title: ''
labels: enhancement
assignees: ''
---

## What's missing

## Where it would live

<!-- HEADER_ALIASES (a new source format / column-name alias), CATEGORY_RULES
(a new (resource_type, field_name, operation) -> category mapping), the
DASHBOARD_TEMPLATE (a new dashboard section/visual), or SKILL.md
(workflow-level, e.g. a new CLI flag). -->

## Stays factual?

<!-- V1 is strictly factual, never judgmental — it states "Campaign X hasn't
changed in 23 days," never "the agency is neglecting Campaign X." A request
that requires scoring, grading, or flagging a change as good/bad/risky is
out of scope for this version; say so explicitly if that's what you're
proposing, so it's tracked as a deliberate future-version idea rather than
silently dropped. -->
